<div class="foots">
  <a id="fb" data-aos="fade-down" href="https://www.facebook.com/Infotsav/"><i class="fab fa-facebook-f fa-3x"></i></a>
  <a id="insta" data-aos="fade-down" href="https://www.instagram.com/infotsav19/?hl=en"><i class="fab fa-instagram fa-3x"></i></a>
  <a id="linkedin" data-aos="fade-down" href="https://www.linkedin.com/company/infotsav-iiit-gwalior"><i class="fab fa-linkedin-in fa-3x"></i></a>
</div>
<div class="copyright">
  <p>©TeamInfinityABVIIITM</p>
</div>
</body>

<footer>
  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  <script>
    AOS.init({
      easing: 'ease-out-back',
      duration: 1500,
      delay: 300,
      disable: 'mobile',
      mirror: true
    });
  </script>
  <script type="text/javascript">
    const ham = $(".ham");
    var numClick = 0;
    ham.click(function() {
      if (numClick == 0) {
        numClick = 1;
        $(".ham #handle").removeClass("rotate-right");
        $(".ham #handle").addClass("rotate-left");
        $(".one").addClass("one-final");
        $(".two").addClass("two-final");
        $(".three").addClass("three-final");
        $(".line").addClass("line-final");
        $(".nav-container").removeClass("slide-up");
        $(".nav-container").addClass("slide-down");
      } else {
        numClick = 0;
        $(".ham #handle").removeClass("rotate-left");
        $(".ham #handle").addClass("rotate-right");
        $(".one").removeClass("one-final");
        $(".two").removeClass("two-final");
        $(".three").removeClass("three-final");
        $(".line").removeClass("line-final");
        $(".nav-container").removeClass("slide-down");
        $(".nav-container").addClass("slide-up");
      }
    });

  

    $(window).on("load", function() {
      $(".loader").fadeOut("slow");
    });

    const moto = $("#moto")[0];
    window.addEventListener("scroll",()=>{
      const scrollValue = window.pageYOffset;
      const rateY = scrollValue*0.55;
      const rateX = scrollValue*0.8;
      moto.style.transition = "0s";
      moto.style.transform = "translate3d("+(-rateX)+"px, "+rateY+"px, 0px)";
      if(scrollValue == 100){
        if(moto.innerText=="REDEFINING"){
          moto.innerText = "REDEFINED";
        }
        else {
          moto.innerText = "REDEFINING";
        }
      }
    });
  </script>

  <script>
    const canvas = document.querySelector("#canvas");

    canvas.width = window.innerWidth - 20;
    canvas.height = (window.innerHeight);

    var c = canvas.getContext('2d');

    var colorArray = [
      "#ebe6e6", "#ededed"
    ];


    function Circle(x, y, radius, dx, dy) {
      this.x = x;
      this.y = y;
      this.radius = radius;
      this.dx = dx;
      this.dy = dy;

      this.draw = function() {
        c.beginPath();
        c.arc(this.x, this.y, this.radius, 0, Math.PI * 2, false);
        c.strokeStyle = colorArray[Math.floor(Math.random() * colorArray.length)];
        c.fillStyle = colorArray[Math.floor(Math.random() * colorArray.length)];
        c.fill();
        c.stroke();
      }

      this.update = function() {
        if (this.x + this.radius > innerWidth - 20 || this.x - this.radius < 0) {
          this.dx = -this.dx;
        }
        if (this.y - this.radius < 0 || this.y + this.radius > innerHeight) {
          this.dy = -this.dy;
        }
        this.x += this.dx;
        this.y += this.dy;

        this.draw();
      }

    }


    var circleArrays = [];

    for (var i = 0; i < 100; i++) {
      var radius = 5;
      var x = Math.random() * ((innerWidth - 20) - (radius * 2)) + radius;
      var y = Math.random() * ((innerHeight) - (radius * 2)) + radius;
      var dx = (Math.random() - 0.5);
      var dy = (Math.random() - 0.5);

      circleArrays.push(new Circle(x, y, radius, dx, dy));
    }

    function animate() {
      requestAnimationFrame(animate);
      c.clearRect(0, 0, innerWidth, innerHeight);

      for (var i = 0; i < circleArrays.length; i++) {
        circleArrays[i].update();
      }
    }

    animate();
  </script>

</footer>

</html>
